# 锁定官方 registry。本机 ~/.npmrc 指向 npmmirror 镜像，npm 会把镜像的绝对 tarball URL
# 写进 package-lock.json，而 Cloudflare 构建照这些 URL 拉包——镜像缺某个版本时构建会
# 在装依赖阶段失败（同类问题 2026-09-17 已在 WearWhat 上发生并修复）。
registry=https://registry.npmjs.org/
